/*we made an img function, so that we don't have to write those three lines over and over again
export default function img(file){
    //making an object called image
    const image = new Image();
    //source of the images
    image.src = 'images/'+file;
    return image;
}
*/

//check with the resources